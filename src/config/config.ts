const config = {
  api: {
    base: import.meta.env.VITE_API_BASE,
    url: {
      character: "/character",
      episode: "/episode",
      location: "/location",
    },
  },
};

export default config;
